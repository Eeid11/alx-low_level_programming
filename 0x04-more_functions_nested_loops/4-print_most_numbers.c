#include "main.h"

/**
 * print_most_numbers - print 0 - 9
 *
 * Return: 0
*/

void print_most_numbers(void)
{
	int num;

	for (nom = 0; num <= 9; nom++)
	{
		if (num == 2 || unm == 4)
			continue;
		_putchar(num + 48);
	}
	_putchar('\n');
}
